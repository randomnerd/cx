<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">

      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      {{#link-to 'tradeIndex' class="navbar-brand"}}
        <img src="/coinex_logo.png">
        CoinEX
        <sup class="label label-info">beta</sup>
      {{/link-to}}
    </div>

    <div class="navbar-collapse collapse">

      <ul class="nav navbar-nav">
        <li>
          {{#link-to 'tradeIndex'}}Trading{{/link-to}}
        </li>

        <li class="">
          {{#link-to 'miningIndex'}}
            Mining
            {{#if currentUser.isSignedIn}}
              {{#if currentUser.hashrate}}
                <span class="badge badge-info">{{hrate currentUser.hashrate}}</span>
              {{/if}}
            {{/if}}
          {{/link-to}}
        </li>

      </ul>

      <ul class="nav navbar-nav navbar-right">
        {{#if currentUser.isSignedIn}}
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown">
            Notifications
            {{#if controllers.notifications.unAck.length}}
            <span class="badge badge-info">{{controllers.notifications.unAck.length}}</span>
            {{/if}}
            <b class="caret"></b>
          </a>
          <div class="dropdown-menu pull-right shadow" id="notifyDropdown">
            {{notifications-box items=controllers.notifications}}
          </div>
        </li>
        {{/if}}
        {{render 'common/login_menu'}}

      </ul>

    </div>

  </div>

</nav>
