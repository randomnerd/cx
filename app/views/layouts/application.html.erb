<!DOCTYPE html>
<html>
<head>
  <title>CoinEx</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="cryptostocks" content="We will be listing CoinEx shares on Cryptostocks.">
  <% if current_user %>
    <meta name="current-user" content="<%= UserSerializer.new(current_user, root: false).to_json %>" />
    <% if masquerading? %>
      <meta name="masquerade" content="<%= unmasq_hq_users_path %>" />
    <% end %>
  <% end %>
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <script src="https://d3dy5gmtp8yhk7.cloudfront.net/2.1/pusher.min.js" type="text/javascript"></script>
  <% if Rails.env.production? %>
    <script>pusher = new Pusher('339899db7460f58950bd', {authEndpoint: '/pusher/auth'})</script>
  <% elsif Rails.env.staging? %>
    <script>pusher = new Pusher('e024a679b66f37d3ed8e', {authEndpoint: '/pusher/auth'})</script>
  <% else %>
    <script>pusher = new Pusher('f432464ea002212eaf37', {authEndpoint: '/pusher/auth'})</script>
  <% end %>
</head>
<body>

<%= yield %>

<%= render partial: 'common/zendesk_support_tab' %>
</body>
</html>
